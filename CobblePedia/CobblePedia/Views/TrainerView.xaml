<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CobblePedia.Views.TrainerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:CobblePedia.Views"
    xmlns:model="using:CobblePedia.ViewModels"
    x:DataType="model:CobblePediaViewModel"
    x:DefaultBindMode="OneTime"
    mc:Ignorable="d">

  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="auto" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition />
    </Grid.RowDefinitions>
    <ScrollViewer Grid.Column="0" Grid.Row="0">
      <ListView SelectionMode="Single" ItemsSource="{x:Bind ViewModel.TrainerViewModels}" 
        SelectedItem="{x:Bind ViewModel.SelectedTrainer, Mode=TwoWay}">
        <ListView.ItemTemplate>
          <DataTemplate x:DataType="model:TrainerViewModel">
            <TextBlock Text="{x:Bind Name, Mode=TwoWay}"
                    Style="{ThemeResource BaseTextBlockStyle}"
                    Margin="12,6,0,0"/>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
    </ScrollViewer>
    <!--<StackPanel Orientation="Horizontal" Grid.Column="1" Grid.Row="0">
      <StackPanel Orientation="Vertical">
        <TextBlock Text="{x:Bind ViewModel.SelectedTrainer.Name, Mode=OneWay}"
                Style="{ThemeResource BaseTextBlockStyle}"
                Margin="12,6,0,0"/>
        <StackPanel Orientation="Vertical">
          <TextBlock Text="Team"
                  Style="{ThemeResource BaseTextBlockStyle}"
                  Margin="12,6,0,0"/>
          --><!--<Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="auto" />
              <ColumnDefinition Width="auto" />
              <ColumnDefinition Width="auto" />
              <ColumnDefinition Width="auto" />
              <ColumnDefinition Width="auto" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
              <RowDefinition />
            </Grid.RowDefinitions>
          </Grid>--><!--
          <ListView SelectionMode="Single" ItemsSource="{x:Bind ViewModel.SelectedTrainer.Teams, Mode=OneWay}">
            <ListView.ItemTemplate>
              <DataTemplate x:DataType="model:TrainerTeamViewModel">
                <StackPanel Orientation="Horizontal">
                  <Image Source="{x:Bind Species.FrontDefault, Mode=OneWay}"
                        Height="32"
                        Margin="4,4,4,4"/>
                  <TextBlock Text="{x:Bind Species.Name, Mode=OneWay}"
                    Style="{ThemeResource BaseTextBlockStyle}"
                    Margin="12,4,12,4"/>
                  <Image Source="{x:Bind Move1.MoveTypeIcon, Mode=OneWay}"
                         Height="32"
                         Margin="4,4,4,4"/>
                  <TextBlock Text="{x:Bind Move1.Name, Mode=OneWay}"
                      Style="{ThemeResource BaseTextBlockStyle}"
                      Margin="12,6,0,0"/>
                  <Image Source="{x:Bind Move2.MoveTypeIcon, Mode=OneWay}"
                         Height="32"
                         Margin="4,4,4,4"/>
                  <TextBlock Text="{x:Bind Move2.Name, Mode=OneWay}"
                      Style="{ThemeResource BaseTextBlockStyle}"
                      Margin="12,6,0,0"/>
                  <Image Source="{x:Bind Move3.MoveTypeIcon, Mode=OneWay}"
                         Height="32"
                         Margin="4,4,4,4"/>
                  <TextBlock Text="{x:Bind Move3.Name, Mode=OneWay}"
                      Style="{ThemeResource BaseTextBlockStyle}"
                      Margin="12,6,0,0"/>
                  <Image Source="{x:Bind Move4.MoveTypeIcon, Mode=OneWay}"
                         Height="32"
                         Margin="4,4,4,4"/>
                  <TextBlock Text="{x:Bind Move4.Name, Mode=OneWay}"
                      Style="{ThemeResource BaseTextBlockStyle}"
                      Margin="12,6,0,0"/>
                </StackPanel>
              </DataTemplate>
            </ListView.ItemTemplate>
          </ListView>
        </StackPanel>
      </StackPanel>
    </StackPanel>-->
  </Grid>

</Page>
